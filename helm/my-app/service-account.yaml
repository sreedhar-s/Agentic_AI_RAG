apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "my-app.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  annotations:
    # IRSA: binds this ServiceAccount to an IAM role so pods can call AWS APIs
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.iamRoleArn }}
